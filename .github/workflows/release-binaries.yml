name: Release Binaries
permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    tags: ['v*.*.*']

jobs:
  release-binaries:
    uses: ./.github/workflows/build-binaries.yml
    secrets: inherit
