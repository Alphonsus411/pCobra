# Backlog técnico de equivalencia (autogenerado)

Fecha de generación: 2026-06-16
Versión contrato: 1.0.0

## Tareas abiertas (status != full)

- [ ] `imports_corelibs` en `javascript` → elevar de `partial` a `full`. Contexto: Sin limitaciones documentadas.
- [ ] `manejo_errores` en `javascript` → elevar de `partial` a `full`. Contexto: Sin limitaciones documentadas.
- [ ] `manejo_errores` en `rust` → elevar de `partial` a `full`. Contexto: Sin limitaciones documentadas.
- [ ] `tipos_compuestos` en `rust` → elevar de `partial` a `full`. Contexto: Sin limitaciones documentadas.

## Regla de mantenimiento

Ejecutar este script tras cualquier cambio en la matriz o backends para mantener el backlog sincronizado.
