func contar_gc(texto):
    var total = 0
    para letra en texto:
        si letra == 'G' o letra == 'C':
            total += 1
        fin
    fin
    retornar (total / longitud(texto)) * 100
fin

secuencia = leer('secuencia.fasta')
imprimir "Porcentaje de GC:", contar_gc(secuencia)
