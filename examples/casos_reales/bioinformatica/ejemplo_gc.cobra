func contar_gc(texto):
    var total = 0

    para letra en texto:
        si letra == 'G':
            total = total + 1
        fin

        si letra == 'C':
            total = total + 1
        fin
    fin

    retorno (total / longitud(texto)) * 100
fin
