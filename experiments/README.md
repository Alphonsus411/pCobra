# Experimentos (fuera de rutas operativas por defecto)

El contenido de esta carpeta **no forma parte del flujo operativo** de pCobra.

- No se registra en CLI ni en los mapeos oficiales de targets.
- No se incluye en rutas públicas de soporte oficial.
- Se conserva únicamente como referencia de prototipos internos.

Actualmente:

- `llvm_backend.py`: prototipo histórico **no operativo** y fuera del contrato oficial.
